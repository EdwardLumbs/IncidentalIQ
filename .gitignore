# ── Node ──────────────────────────────────────────────
node_modules/
.env
.env.*
*.log

# ── Android / Gradle build output ─────────────────────
# Compiled output, intermediates, and the APK itself — all regenerated by a build.
build/
.gradle/

# Machine-specific SDK path — breaks on any other machine if committed.
local.properties

# ── Android Studio / IDE ──────────────────────────────
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# ── Keystores / signing (never commit secrets) ────────
*.jks
*.keystore
